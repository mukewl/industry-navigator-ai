# Industry Navigator AI — project context

This document is for AI assistants and developers working in this repo. It summarizes stack, architecture, design choices, what ships today, and what is still open.

## Product intent

**Orange Business–branded internal demo** for a "Gen AI sustainability profiling" workflow: account managers pick a company and read a structured **sustainability brief** (challenges, mapped Orange solutions, export hooks). The experience is aimed at **enterprise sales reps** (dense UI, minimal marketing chrome).

---

## Tech stack

| Layer | Choice |
|--------|--------|
| Build | **Vite 5** (`@vitejs/plugin-react-swc`) |
| UI | **React 18** + **TypeScript** |
| Styling | **Tailwind CSS 3.4** + **tailwindcss-animate** |
| Components | **shadcn/ui** pattern — **Radix UI** primitives under `src/components/ui/` |
| Icons | **lucide-react** |
| Routing | **react-router-dom** v6 — only `/` and `*` are used in practice |
| Server state | **TanStack Query** — `QueryClientProvider` in `App.tsx`; main screens do not depend on it yet |
| Charts | **Recharts** — used in `AccountDashboard` (bar chart) and `SustainabilityBrief` Overview tab (impact score bar chart) |
| Backend (scaffold) | **Supabase** — `@supabase/supabase-js`, client in `src/integrations/supabase/client.ts`, generated `types.ts` |
| Forms / validation | **react-hook-form**, **zod**, **@hookform/resolvers** (available; not central to current flows) |
| Toasts | **shadcn Toaster** + **Sonner** (both mounted in `App.tsx`) |
| Animation | **CSS keyframes preferred**; framer-motion is allowed for complex animations where CSS keyframes would be impractical (e.g., SVG path animations, spring physics) |

**Path alias:** `@/` → `src/` (see `vite.config.ts`, `tsconfig`).

**Dev server:** Vite defaults to port **8080**, host `::` (see `vite.config.ts`).

---

## Repository layout (high signal)

```
src/
  App.tsx                 # Router shell, providers, toasters
  main.tsx
  index.css               # Tailwind base + HSL tokens + glass utility classes + custom keyframes
  pages/
    Index.tsx             # Main app: tab state + view switching (heart of navigation)
    NotFound.tsx
  components/
    layout/               # Sidebar, Header
    dashboard/            # AccountDashboard, RoadmapView (+ unused legacy widgets)
    search/               # SearchPanel (new profile entry / hero page)
    architecture/         # ArchitectureView (mounted on the `architecture` tab)
    brief/                # SustainabilityBrief, SolutionDetail
    agents/               # AgentStatusPanel (legacy, not mounted)
    ui/                   # shadcn primitives
    NavLink.tsx           # Small link helper used by layout components
  data/
    briefData.ts          # Static brief dataset — all 10 companies (see below)
    winStrategyData.ts    # Per-solution Win Strategy content (Why Now / Why OBS / How to Win)
  hooks/
    use-mobile.tsx        # Viewport breakpoint hook
    use-toast.ts          # shadcn toast hook
  lib/
    briefMetrics.ts       # Derived stats for dashboard (Renault + Carrefour only)
    companyLogos.tsx      # CompanyLogo component + COMPANY_DOMAINS + COMPANY_NAME_TO_DOMAIN maps
    utils.ts              # cn() etc.
  integrations/supabase/
    client.ts             # createClient — expects VITE_SUPABASE_* env vars
    types.ts              # Database types (generated)
design-system/
  industry-navigator/
    MASTER.md             # Generated by ui-ux-pro-max skill — design system reference (font, spacing, shadows, anti-patterns)
    pages/                # Per-page overrides (empty currently)
```

---

## Navigation and state model

**Important:** Most "screens" are **not separate routes**. `Index.tsx` keeps `activeTab` as a string and renders one of several views. Sidebar / mobile header call `onTabChange`.

**Known `activeTab` values** (from `Index.tsx` + `Sidebar.tsx`):

| Tab id | View |
|--------|------|
| `dashboard` | `AccountDashboard` |
| `search` | `SearchPanel` |
| `brief` | `SustainabilityBrief` (uses `searchQuery` as company name) |
| `renault-brief` | `SustainabilityBrief` with `companyName="Renault"` |
| `carrefour-brief` | `SustainabilityBrief` with `companyName="Carrefour"` |
| `stellantis-brief` | `SustainabilityBrief` with `companyName="Stellantis"` |
| `totalenergies-brief` | `SustainabilityBrief` with `companyName="TotalEnergies"` |
| `saintgobain-brief` | `SustainabilityBrief` with `companyName="Saint-Gobain"` |
| `schneiderelectric-brief` | `SustainabilityBrief` with `companyName="Schneider Electric"` |
| `veolia-brief` | `SustainabilityBrief` with `companyName="Veolia"` |
| `airfranceklm-brief` | `SustainabilityBrief` with `companyName="Air France-KLM"` |
| `danone-brief` | `SustainabilityBrief` with `companyName="Danone"` |
| `loreal-brief` | `SustainabilityBrief` with `companyName="L'Oréal"` |
| `roadmap` | `RoadmapView` (Phase 2 placeholder) |
| `architecture` | `ArchitectureView` (from `components/architecture/`) |

The `BRIEF_TAB_MAP` in `Index.tsx:12` is the single source of truth for these tab IDs and the company-name strings the brief receives. The reverse map (`NAME_TO_TAB`) lets the search input route directly to the matching tab.

**Other state in `Index`:** `searchQuery`, `previousTab`, `hasSearched`, `recentIds`. There is no `isLoading` state — `SearchPanel` receives `isLoading={false}` literally (`Index.tsx:107`), so the "Running" button state is dead. Search submits go straight to `brief`; there is no longer an intermediate pipeline step.

**Brief routing:** `SustainabilityBrief` resolves company data via a `nameMap` object with explicit keys for all 10 companies. If the company name doesn't match any key, `data` is `null` and a proper empty state is shown — **no silent Renault fallback**.

---

## Data and "business logic"

- **Source of truth for copy/structure:** `src/data/briefData.ts` — challenges, solutions, benchmarks, contacts, financial blocks, etc.
- **Companies in briefData:** `renault`, `carrefour`, `stellantis`, `totalenergies`, `saintgobain`, `schneiderelectric`, `veolia`, `airfranceklm`, `danone`, `loreal` — **all 10 are fully populated**.
- **Solution data shape** (important for Overview additions and SolutionDetail):
  ```ts
  {
    challengeId: number,
    challengeLabel: string,
    product: string,
    detail: string,
    icon: LucideIcon,
    impactScore: number,           // 0–100, used in Overview bar chart + Top Play card
    metrics: {
      profitability: string,       // e.g. "€2.4M estimated contract value | High margin service"
      clientBenefit: string,
      co2Impact: string,           // e.g. "-12,400 tCO2e/year estimated"
    },
    details: {
      overview: string[],
      financialCase: {
        contractValue: string,     // e.g. "€2.4M" — used in Top Play card
        revenueType: string,
        upsellPotential: string,
        years: { year: string, revenue: string }[],
      },
      esgCase: string[],
      keySellingPoints: string[],
      nextSteps: string[],
    }
  }
  ```
- **Contact data shape:** `{ role: string, icon: LucideIcon }` — roles are displayed in Overview "Key contacts" and Export "Targets" sections.
- **Dashboard aggregates:** `src/lib/briefMetrics.ts` — e.g. summed contract labels, CO₂ string, `BRIEF_COMPANY_KEYS`. **This file still only references `renault` and `carrefour`** — it has not been updated to include all 10 companies. This is intentional (dashboard is a two-client view); update carefully if expanding the dashboard client list.
- **No live API** in the main user path: search submits jump straight to the brief; export and CRM actions show **toasts only**.

**Supabase:** Client is ready (`VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`). **No feature code in the current UI imports or queries it** — wiring persistence, auth, or real profiles is future work.

---

## Design system (current — updated 2026-04-26)

- **Theme:** The app is **always light mode**. `index.html` does **not** set `class="dark"`, and nothing in `src/` toggles it — the `.dark { ... }` token block in `src/index.css` (line ~111) is currently unused dead code, kept in case a dark variant is reintroduced.
- **Background:** Page wrapper in `Index.tsx:147` is `bg-[#f5f5f7]` (matches the `:root` `--background: 240 5% 97%`). Content cards sit on top as solid white with subtle borders/shadows. The "frosted glass" utilities resolve to `rgba(255,255,255,…)` on this light background — they read as soft white panels, not as dark glass.
- **Palette:** Zinc / slate neutrals + **single chromatic accent — Orange `#FF6900`** (`--primary: 24 100% 50%`). No other chromatic colours in the UI chrome.
- **Typography:** **Plus Jakarta Sans** (Google Fonts, weights 300–800) loaded via `@import` in `index.css`. JetBrains Mono for code only. SF Pro system stack is kept as fallback. Letter-spacing on body is `-0.014em` (tuned for Plus Jakarta Sans; was -0.022em for SF Pro).
  - Custom utility classes in `index.css` `@layer components`:
  - `type-eyebrow` — 12px uppercase metadata
  - `type-section-label` — 12px uppercase section headers
  - `type-prose` / `type-prose-md` / `type-ui` — readable body density
  - **New section label pattern used in redesigned components:** `text-[0.625rem] font-semibold uppercase tracking-[0.06em] text-muted-foreground/60` — inline, not a utility class
- **Border radius:** `--radius: 0.75rem` (12px). Changed from 8px. `rounded-lg` = 12px, `rounded-md` = 10px, `rounded-sm` = 8px.
- **Glass utility classes** (defined in `index.css`, still valid):
  - `glass-nav` — dark glass for sidebar rail (still used)
  - `glass-panel` — light frosted surface (78% white)
  - `glass-panel-strong` — heavier frosted surface (88% white)
  - `glass-inset` — recessed frosted inset (55% white)
  - `glass-tab-shell` — **deprecated in practice** — tab bar now uses plain `bg-black/[0.03]` border approach
  - `glass-stat` — stat pill
- **Card elevation pattern (non-glass):** `bg-white rounded-xl border border-black/[0.06] shadow-[0_2px_8px_rgba(0,0,0,0.06)]` — used in dashboard stat cards, POC card, presentation card.
- **Shadows:** Apple shadows (`shadow-apple`, `shadow-apple-soft`) kept for glass panels. MASTER.md shadow scale added as CSS variables (`--shadow-ds-sm/md/lg/xl`) for reference. Inline `shadow-[...]` is acceptable for one-off elevations.
- **Sidebar active state:** Uses `shadow-[inset_2px_0_0_0_hsl(24_100%_50%)]` for the left orange accent indicator — no layout shift vs. using `border-l`.
- **Button:** `active:scale-[0.97]` added to base; `transform` added to transition list. All variants have smooth press feedback.
- **Company logos:** `src/lib/companyLogos.tsx` exports `CompanyLogo` component, `COMPANY_DOMAINS` (key→domain), `COMPANY_NAME_TO_DOMAIN` (name→domain), `clearbitLogoUrl`. Logo shows immediately with initials fallback underneath; Clearbit image fades in on load. Used in: SearchPanel autocomplete, AccountDashboard sidebar list, SustainabilityBrief header.
- **Animation classes** in `index.css` `@layer utilities`:
  - `animate-fade-in` — 0.45s fade, used on page/view entry
  - `animate-slide-up` — 0.45s slide up + fade
  - `animate-tab-from-right` — 0.2s slide from +20px, forward tab transitions
  - `animate-tab-from-left` — 0.2s slide from −20px, backward tab transitions
- **Design system reference:** `design-system/industry-navigator/MASTER.md` — generated by `ui-ux-pro-max` skill. Specifies Plus Jakarta Sans, Trust & Authority style, Enterprise Gateway pattern, shadow scale, and anti-patterns. **Brand override:** keep `#FF6900` as accent (MASTER suggests `#DC2626` red — ignore that).
- **Favicon:** SVG data URI in `index.html` — orange rounded square with white "O".
- **Explicit non-goals:** no hero marketing layout, no AI purple/pink gradients, no gradient text on headings — tool-like, enterprise-grade density.

---

## What is built (feature checklist)

- [x] **Dashboard** — Two-panel layout: scrollable client list (Renault / Carrefour), selection, summary stats, Recharts bar chart, **Open full brief** CTA.
- [x] **New profile / hero page** — Redesigned: full-bleed background with radial orange glow + dot grid, large headline "Generate a sustainability brief.", search card, Solution Type filter only (Recent Briefs, Available Companies, ESG Risk filter, and Sector filter removed). Two-tier container width (44rem outer, 32rem inner for card).
- [x] **Brief — tab animation** — Tab switching uses directional CSS slide animation (right for forward, left for backward). Tabs component is controlled; content re-mounts on switch via `key`.
- [x] **Brief — Overview tab** — Meta row + pressure/play table + **impact score bar chart** (Recharts) + **Top Recommended Play card** (highest impactScore, contract value, CO₂ impact, link to playbook). Key Contacts and OBS Custom Capabilities (customSolutions preview) have been **removed** from this tab.
- [x] **Brief — Challenges tab** — Redesigned as a table with columns: #, Challenge, Urgency badge, Solution Type (pulled from matched solution's `product` field), Description (truncated). Replaces the previous collapsible cards.
- [x] **Brief — Solutions tab** — Cards → `SolutionDetail` playbook view. Per-solution **Win Strategy** content lives in `src/data/winStrategyData.ts` and is surfaced inside `SolutionDetail` (Why Now / Why OBS / How to Win).
- [x] **Brief — Export tab** — Checkboxes, PowerPoint/Canva toasts, Save to CRM toast, confidence bar, benchmarks, targets. **Next Steps** section moved here from Overview.
- [x] **Brief — empty state** — Proper "no brief available" screen for unrecognised companies; lists all 10 supported names.
- [x] **POC card** — Fixed.
- [x] **Back button** — Fixed (returns correctly from SolutionDetail to Solutions list).
- [x] **Sidebar — Library with Recent + solution-type groups** — Recent section (up to 3, persisted to localStorage) + three collapsible solution-type groups (Smart Eco Energy, Evolution Platform, Ocean + Circular Mobility). Group membership is derived dynamically from `briefData` — a company only appears under a group if it has a solution whose `product` field matches. Groups with no members are hidden.
- [x] **Roadmap** — Static Phase 2 feature list.
- [x] **System architecture** — Separate informational view.
- [x] **Responsive shell** — Sidebar (lg+), sheet + header on small screens.
- [x] **404** — `NotFound` route for unknown paths.

### Build status (2026-04-26, `visual-experiment` branch)

Working branch is `visual-experiment` (tracks `origin/visual-experiment`). The `final-changes` branch referenced in earlier sessions has been superseded — the visual-redesign work that lived there landed in commits like `6c50a22 visual redesign - plus jakarta sans, premium UI pass`. Do not touch `master`.

### Remaining tasks

- [ ] **Win Strategy modal scrolling** — Fix scrolling in the Win Strategy modal inside SolutionDetail. Two approved approaches: (a) add tabs inside the modal for Why Now / Why OBS / How to Win so no scrolling is needed, or (b) make the modal taller with `max-h-screen` and `overflow-y-auto`. Either is acceptable — pick whichever is cleaner for the existing markup.
- [ ] **Push all changes to GitHub** — Push `visual-experiment` branch to remote.

---

## Recently changed files (session — 2026-04-16)

| File | What changed |
|------|-------------|
| `src/index.css` | Font → Plus Jakarta Sans (Google Fonts). Radius token 8px → 12px. Body letter-spacing -0.022em → -0.014em. Dark bg pure black → `222 20% 5%`. Added MASTER.md shadow scale vars. Added global `cursor: pointer` for buttons. Added `scroll-behavior: smooth`. |
| `src/lib/companyLogos.tsx` | **New file.** `CompanyLogo` component (initials-first, Clearbit logo fades in on load). `COMPANY_DOMAINS` (key→domain) and `COMPANY_NAME_TO_DOMAIN` (name→domain) maps for all 10 companies. |
| `src/components/ui/button.tsx` | Added `active:scale-[0.97]`; added `transform` to transition list. |
| `src/components/search/SearchPanel.tsx` | Visual redesign: heavier headline (font-extrabold), better search card (white + shadow), taller input/button (h-12), larger autocomplete logos, enterprise category section with section label and square `rounded-lg` chips. |
| `src/components/dashboard/AccountDashboard.tsx` | "Open full brief" button moved to header row (right-aligned). Full visual redesign: clean white card container, polished filter chips (rounded-md + border), company list active state uses inset left-orange box-shadow, stat cards → `bg-white` + drop shadow + `font-bold` numbers, consistent section label treatment throughout. |
| `src/components/layout/Sidebar.tsx` | Visual redesign: two-line header lockup, `font-medium` nav items, `h-4 w-4` icons, inset left-orange shadow for active state, consistent uppercase section labels, DRY nav item classes. |
| `src/components/brief/SustainabilityBrief.tsx` | Header: larger logo (h-10 w-10 rounded-xl), `font-bold` headline, orange eyebrow label. Tab bar: segmented-control style (white active tab, drop shadow, plain background instead of glass). |
| `index.html` | Added SVG favicon (orange rounded square), Google Fonts preconnect hints. |
| `tailwind.config.ts` | Font families updated to Plus Jakarta Sans as first entry in sans + display stacks. |
| `design-system/industry-navigator/MASTER.md` | **New file** — generated by `ui-ux-pro-max` skill. Design system reference: Plus Jakarta Sans, Trust & Authority style, Enterprise Gateway pattern, shadow scale, anti-patterns. |

## Previously changed files (prior sessions)

| File | What changed |
|------|-------------|
| `src/components/search/SearchPanel.tsx` | Stripped to search input + Solution Type filter. Redesigned: background decoration layers, enlarged headline, two-tier container (44rem / 32rem), all 10 chips |
| `src/components/brief/SustainabilityBrief.tsx` | Controlled tabs with directional slide animation; bar chart; silent Renault fallback replaced with null/empty state; Win Strategy tab in SolutionDetail; Challenges redesigned as table |
| `src/components/brief/SolutionDetail.tsx` | Win Strategy tab added; POC card fixed; back button fixed |
| `src/components/layout/Sidebar.tsx` | Library: Recent (localStorage, up to 3) + solution-type groups derived dynamically from `briefData` |
| `src/index.css` | Added tab slide keyframes; glass utility classes |

---

## Patterns and decisions established this session

### Tab animation pattern
`SustainabilityBrief` uses **controlled Radix `Tabs`** (`value` + `onValueChange`) but does **not** use `TabsContent` — content is rendered manually in a single `div` with `key={slideKey}`. Incrementing `slideKey` forces a React re-mount, which retriggers the CSS animation. Direction is computed by comparing the incoming tab's index in `TAB_ORDER` against the current tab's index.

```ts
const TAB_ORDER = ["overview", "challenges", "solutions", "export"] as const;

const handleTabChange = (tab: string) => {
  const fromIdx = TAB_ORDER.indexOf(tab as TabId);   // destination
  const toIdx   = TAB_ORDER.indexOf(activeTab);       // current
  setSlideDir(fromIdx > toIdx ? "right" : "left");
  setActiveTab(tab as TabId);
  setSlideKey((k) => k + 1);
};
```

Apply this same pattern if other multi-tab views need directional animation in future.

### SearchPanel two-tier width
The hero page uses a **nested container strategy** to let chips span a wider column than the card:
- Outer: `max-w-[44rem]` — chip section uses this full width
- Inner: `max-w-[32rem]` — heading and search card are narrower for readability

### Bar chart data pattern (Overview)
Chart data is derived inline from `data.solutions` before the return:
```ts
const chartData = data.solutions.map((s) => ({
  label: `#${s.challengeId}`,   // short X-axis label
  fullName: s.product,           // shown in tooltip
  score: s.impactScore,
}));
```
Tooltip uses `payload[0].payload` cast to `(typeof chartData)[number]` to show `fullName`.

### Top Recommended Play
`topPlay` is derived by sorting solutions descending on `impactScore`:
```ts
const topPlay = [...data.solutions].sort((a, b) => b.impactScore - a.impactScore)[0];
```
Contract value is accessed via `(topPlay as any).details?.financialCase?.contractValue` with `?? "—"` fallback because TypeScript's inference through the union of all `briefData` values is not always reliable for deeply nested properties.

### Recharts styling conventions
Match the existing `AccountDashboard` chart style:
- `CartesianGrid vertical={false}` with `stroke="rgba(0,0,0,0.06)"`
- `axisLine={false}`, `tickLine={false}` on both axes
- Tick fill: `hsl(240 3.8% 46.1%)` (matches `--muted-foreground`)
- Bar fill: `hsl(24 100% 50%)` (matches `--primary`) with `radius={[4, 4, 0, 0]}`
- Custom tooltip: `rounded-lg border border-black/[0.06] bg-white/95 px-3 py-2 text-[0.8125rem] shadow-apple-sm backdrop-blur-xl`

---

## Legacy / unused in main flow

These files are **not** mounted from `Index.tsx` today:

- `dashboard/TrendsDashboard.tsx`, `dashboard/ExecutiveSummary.tsx`, `dashboard/CompanyAnalysis.tsx`, `dashboard/CapabilityMap.tsx`, `dashboard/Recommendations.tsx`
- `dashboard/SystemArchitecture.tsx` — superseded by `architecture/ArchitectureView.tsx`, which **is** mounted on the `architecture` tab
- `agents/AgentStatusPanel.tsx`

Likely Lovable-scaffold leftovers. Safe to remove or reintegrate intentionally — do not assume they are dead without checking imports.

---

## Known gaps and suggested next work

### Highest priority (product completeness)

1. **`briefMetrics.ts` covers only 2 companies** — Dashboard stats (contract sum, CO₂ label) only aggregate Renault + Carrefour. If the dashboard expands to show all clients, this file needs updating.
2. **Search `isLoading`** — `Index.tsx:107` passes `isLoading={false}` literally; the "Running" button state in `SearchPanel` is dead. Either remove the prop or wire it to a real async step.
3. **Export** — Implement real PowerPoint / Canva generation; wire checkboxes to actual selection state (currently `defaultChecked` only).

### Product / backend

4. **Supabase** — Persist profiles, users, brief versions; RLS; optional real "Save to CRM" integration.

### UX / IA

5. **URL sync** — Deep-link tabs or briefs (`/brief/renault`) for share/bookmark. Currently all navigation is in-memory state only.
6. **Nav duplication** — Sidebar has both Dashboard entry and direct "Renault Brief" / "Carrefour Brief" links — confirm IA with stakeholders before expanding to all 10.

### Engineering

7. **Bundle size** — Recharts inflates main chunk (~887KB gzipped ~259KB). Consider `React.lazy` for dashboard and brief charts.
8. **Tests** — No test suite; add e2e or critical unit tests if product hardens.
9. **ESLint** — `npm run lint` exists; keep CI-aligned if added.

### Housekeeping

10. **`package.json` name** — Still `vite_react_shadcn_ts`; rename if publishing.
11. **`scripts/extract-brief-data.mjs`** — Utility used to extract `briefData.ts`; optional to delete or document.

---

## Conventions for agents

- Prefer **`@/` imports** for `src`.
- **Do not break** `Index` tab contract without updating `Sidebar` / `Header` / any `setActiveTab` callers.
- **Brief content** edits: start from `src/data/briefData.ts` — keep `SolutionData` / structure compatible with `SolutionDetail`. Do not add fields to some companies without adding them to all 10.
- **Design changes:**
  - Read `design-system/industry-navigator/MASTER.md` before any visual work.
  - Use `glass-nav` for the sidebar rail. For content cards use `bg-white rounded-xl border border-black/[0.06] shadow-[0_2px_8px_rgba(0,0,0,0.06)]` — not `glass-panel-strong`.
  - Section labels: `text-[0.625rem] font-semibold uppercase tracking-[0.06em] text-muted-foreground/60` (inline, not a utility class).
  - Sidebar active state: `shadow-[inset_2px_0_0_0_hsl(24_100%_50%)]` — no layout shift.
  - Company logos: import `CompanyLogo` from `@/lib/companyLogos` — never render raw `<img>` for company logos.
  - Avoid hard-coded hex **except** where Recharts requires explicit colour strings (use `hsl(24 100% 50%)` for primary, `hsl(240 3.8% 46.1%)` for muted).
  - Brand colour is always `#FF6900` / `hsl(24 100% 50%)` — ignore any design system recommendation to use red (`#DC2626`) as the CTA colour.
- **Animation:** prefer CSS keyframes in `src/index.css` `@layer utilities` block (named `animate-*`); reach for framer-motion only when keyframes are impractical (see Tech stack row for policy).
- **Skills:** The repo includes Claude skills under `.claude/skills/` — use when the user invokes them via `/skill-name`. The `ui-ux-pro-max` skill is installed; its `search.py` script lives at `C:\Users\msbar\AppData\Local\Temp\skills-VoDoiq\src\ui-ux-pro-max\scripts\search.py` (run with `python`, not `python3`).

---

## Quick commands

```bash
npm install
npm run dev      # Vite dev server (port 8080 by default)
npm run build
npm run preview
npm run lint
```

**Env (Supabase):** set `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY` for any future integration that uses `client.ts`.

---

**Active branch:** `visual-experiment` — all current work lives here. **Do not touch `master`.**

*Last updated: 2026-04-26. Remaining: Win Strategy modal scrolling fix, push `visual-experiment` to remote. CLAUDE.md itself has uncommitted edits (this update) on the working tree.*
